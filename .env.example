# Spring MCP Server - Environment Configuration
# Copy this file to .env and fill in your values
# Usage: source .env && ./gradlew bootRun

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# PostgreSQL connection settings (required for production)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=spring_mcp
DB_USER=postgres
DB_PASSWORD=postgres

# =============================================================================
# SECURITY CONFIGURATION
# =============================================================================
# Admin credentials for the web UI
ADMIN_USER=admin
ADMIN_PASSWORD=changeme

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================
SERVER_PORT=8080

# =============================================================================
# GITHUB API CONFIGURATION (Optional but Recommended)
# =============================================================================
# GitHub Personal Access Token for increased API rate limits
# Without token: 60 requests/hour (unauthenticated)
# With token: 5,000 requests/hour (authenticated)
#
# To create a token:
# 1. Go to https://github.com/settings/tokens
# 2. Click "Generate new token (classic)"
# 3. Give it a name like "spring-mcp-server"
# 4. Select scope: "public_repo" (read-only access to public repos)
# 5. Click "Generate token"
# 6. Copy the token and paste it below
#
# IMPORTANT: Keep your token secret! Never commit it to version control.
GITHUB_TOKEN=

# =============================================================================
# FEATURE FLAGS (Optional)
# =============================================================================
# Enable/disable specific features
# BOOTSTRAP_DOCS=false
# OPENREWRITE_ENABLED=true
# LANGUAGE_EVOLUTION_ENABLED=true
# FLAVORS_ENABLED=true
# INITIALIZR_ENABLED=true
# JAVADOCS_ENABLED=true
